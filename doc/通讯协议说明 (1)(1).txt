通讯协议：MSC8328做主
数据传输一共使用三根线传输(分别是CS , MISO , CLK)。


读取完整数据包信息从头到尾依次为：

	歌曲名长度（一个字节）+歌曲名。



每个字节读取9Bit，组成如下：最高位（奇偶校验位）+后八位（传输的数据）
	从机须在时钟的上升沿发送一位，主机在时钟的下降沿读取一位数据，


校验方式:奇校验。如读取数据有误，主机将片选拉高，停止读取，然后复位后重新读取
	添加片选的主要目的是规范读取的开始与结束
